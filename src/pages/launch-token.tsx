import ComingSoon from "@/components/coming-soon";

export default function LaunchToken() {
  return (
    <ComingSoon
      title="Token launches are coming soon"
      description="Fair-launch token creation is in active development. Join the waitlist on the homepage to get notified the moment it opens."
    />
  );
}
